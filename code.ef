object player
{
    str name = "alex";
    byte age = 20;
    byte tag = 'a';
    
    decl to_string()
    {
        if(name == null || age <= 0)
            return null;

        return "name is $s, age is $d and tag $c"(name, age, tag);
    }
}
