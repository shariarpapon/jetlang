- theres is some information boundary overlap issues when logging.
    -- compiler errors should not propagate source code line/col data, rather only the metadata in the input stream.
    -- maybe theres a good reason to not have all logs output locational metadata. 
    -- makes sense only for assert to do that

- invalid token fallback system
    * sync to next end of statement
